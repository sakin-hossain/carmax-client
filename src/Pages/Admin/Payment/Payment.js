import React from 'react';

const Payment = () => {
    return (
        <div>
            <h1 className="mt-5 text-danger">Payment will be added as soon as possible</h1>
        </div>
    );
};

export default Payment;